<?xml version="1.0" encoding="UTF-8"?>
<project name="s2jdbc-gen-core-it" default="showRdbmsName" basedir=".">
	
	<property name="rdbms" value="hsql"/>
	<property name="temp" value="${rdbms}/temp"/>
	<property name="build" value="${temp}/build"/>
	<property name="testresult" value="${temp}/testresult"/>
	<property name="src" value="${temp}/src"/>
	<property name="resources" value="${rdbms}/resources"/>
	<property name="db" value="${temp}/db"/>
	<property name="migrate" value="${db}/migrate"/>
	<property name="dump" value="${db}/dump"/>	
	<property name="ddl-info" value="${db}/ddl-info.txt"/>
	<property name="sql" value="${rdbms}/sql"/>	
	<property name="rootpackagename" value="example"/>
	
	<path id="s2jdbc-gen-classpath">
		<pathelement location="${build}"/>
		<pathelement location="../../s2jdbc-gen-core/build/classes"/>
		<pathelement location="../../seasar2/build/classes"/>
		<pathelement location="../../s2-tiger/build/classes"/>
		<fileset dir="../lib">
			<exclude name="**/*-sources.jar"/>
		</fileset>
		<fileset dir="../../s2jdbc-gen-core/lib">
			<exclude name="**/*-sources.jar"/>
		</fileset>
		<fileset dir="../../seasar2/lib">
			<exclude name="**/*-sources.jar"/>
		</fileset>
		<fileset dir="../../s2-tiger/lib">
			<exclude name="**/*-sources.jar"/>
		</fileset>
	</path>

	<taskdef resource="s2jdbc-gen-task.properties" classpathref="s2jdbc-gen-classpath"/>
	
	<target name="showRdbmsName">
		<echo>${rdbms}</echo>
	</target>

	<target name="testFromDbToEntity" depends="showRdbmsName, clean">
		<!-- setUp -->
		<exec-sql>
			<sqlFileList dir="${sql}/testFromDbToEntity">
				<file name="create.sql"/>
			</sqlFileList>
		</exec-sql>
		<!-- test -->
		<gen-entity
			rootpackagename="${rootpackagename}"
			javafiledestdir="${src}"
		/>
		<javac 
			srcdir="${src}"
			destdir="${build}"
			classpathref="s2jdbc-gen-classpath"
			encoding="UTF-8"
			fork="true"
			debug="on"
		/>
		<gen-ddl 
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
		/>
		<!-- tearDown -->
		<exec-sql>
			<sqlFileList dir="${sql}/testFromDbToEntity">
				<file name="drop.sql"/>
			</sqlFileList>
		</exec-sql>
	</target>
	
	<target name="testFromEntityToDb" depends="showRdbmsName, clean">
		<!-- setUp -->
		<javac 
			srcdir="../src/main/java"
			destdir="${build}"
			classpathref="s2jdbc-gen-classpath"
			encoding="UTF-8"
			fork="true"
			debug="on"
		/>
		<!-- test -->
		<gen-test
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			javafiledestdir="${src}"
		/>
		<gen-condition
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			javafiledestdir="${src}"
			overwrite="true"
		/>
		<gen-service
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			javafiledestdir="${src}"
		/>
		<javac 
			srcdir="${src}"
			destdir="${build}"
			classpathref="s2jdbc-gen-classpath"
			encoding="UTF-8"
			fork="true"
			debug="on"
		/>
		<gen-ddl 
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
		/>
		<migrate 
			classpathDir="${build}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
			rootpackagename="${rootpackagename}"
		/>
		<junit haltonfailure="no" printsummary="true">
			<formatter type="xml"/>
			<classpath refid="s2jdbc-gen-classpath"/>
			<batchtest todir="${testresult}" fork="true">
				<fileset dir="${src}">
					<include name="**/AddressTest.java"/>
				</fileset>
			</batchtest>
		</junit>
		<!-- tearDown -->
		<migrate 
			classpathDir="${build}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
			version="0"
			rootpackagename="${rootpackagename}"
		/>
	</target>

	<target name="testMigrate" depends="showRdbmsName, clean">
		<!-- setUp -->
		<javac 
			destdir="${build}"
			classpathref="s2jdbc-gen-classpath"
			encoding="UTF-8"
			fork="true"
			debug="on">
			<src path="../src/main/java"/>
		</javac>
		<!-- test -->
		<gen-ddl 
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
		/>
		<migrate 
			classpathDir="${build}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
			rootpackagename="${rootpackagename}"
		/>
		<exec-sql>
			<sqlFileList dir="${sql}/testDumpData">
				<file name="insert.sql"/>
			</sqlFileList>
		</exec-sql>
		<gen-ddl 
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
		/>
		<migrate 
			classpathDir="${build}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
			rootpackagename="${rootpackagename}"
		/>
		<!-- tearDown -->
		<migrate 
			classpathDir="${build}"
			migratedir="${migrate}"
			ddlInfofile="${ddl-info}"
			version="0"
			rootpackagename="${rootpackagename}"
		/>
	</target>

	<target name="testDumpDbMeta" depends="showRdbmsName, clean">
		<!-- setUp -->
		<exec-sql>
			<sqlFileList dir="${sql}/testDumpDbMeta">
				<file name="create.sql"/>
			</sqlFileList>
		</exec-sql>
		<!-- test -->
		<dump-db-meta/>
		<!-- tearDown -->
		<exec-sql>
			<sqlFileList dir="${sql}/testDumpDbMeta">
				<file name="drop.sql"/>
			</sqlFileList>
		</exec-sql>
	</target>

	<target name="testDumpData" depends="showRdbmsName, clean">
		<!-- setUp -->
		<javac 
			srcdir="../src/main/java"
			destdir="${build}"
			classpathref="s2jdbc-gen-classpath"
			encoding="UTF-8"
			fork="true"
			debug="on"
		/>
		<exec-sql>
			<sqlFileList dir="${sql}/testDumpData">
				<file name="create.sql"/>
				<file name="insert.sql"/>
			</sqlFileList>
		</exec-sql>
		<!-- test -->
		<dump-data
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			dumpdir="${dump}"
		/>
		<!-- tearDown -->
		<exec-sql>
			<sqlFileList dir="${sql}/testDumpData">
				<file name="drop.sql"/>
			</sqlFileList>
		</exec-sql>
	</target>

	<target name="testLoadData" depends="showRdbmsName, clean">
		<!-- setUp -->
		<javac 
			srcdir="../src/main/java"
			destdir="${build}"
			classpathref="s2jdbc-gen-classpath"
			encoding="UTF-8"
			fork="true"
			debug="on"
		/>
		<exec-sql>
			<sqlFileList dir="${sql}/testDumpData">
				<file name="create.sql"/>
				<file name="insert.sql"/>
			</sqlFileList>
		</exec-sql>
		<dump-data
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			dumpdir="${dump}"
		/>
		<exec-sql>
			<sqlFileList dir="${sql}/testDumpData">
				<file name="drop.sql"/>
				<file name="create.sql"/>
			</sqlFileList>
		</exec-sql>
		<!-- test -->
		<load-data
			classpathDir="${build}"
			rootpackagename="${rootpackagename}"
			dumpdir="${dump}"
		/>
		<!-- tearDown -->
		<exec-sql>
			<sqlFileList dir="${sql}/testDumpData">
				<file name="drop.sql"/>
			</sqlFileList>
		</exec-sql>
	</target>

	<target name="clean">
		<delete dir="${temp}"/>
		<mkdir dir="${build}"/>
		<mkdir dir="${testresult}"/>
		<copy todir="${build}">
			<fileset dir="${resources}">
				<include name="**"/>
			</fileset>
		</copy>
	</target>
</project>
